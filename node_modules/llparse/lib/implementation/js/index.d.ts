import * as frontend from 'llparse-frontend';
export interface IJSCompilerOptions {
    readonly debug?: string;
    readonly module?: 'esm' | 'commonjs';
}
export interface IJSPublicOptions {
    readonly module?: 'esm' | 'commonjs';
}
export declare class JSCompiler {
    readonly options: IJSCompilerOptions;
    constructor(container: frontend.Container, options: IJSCompilerOptions);
    compile(info: frontend.IFrontendResult): string;
    private restartSpans;
    private executeSpans;
}
