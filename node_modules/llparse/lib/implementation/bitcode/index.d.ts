/// <reference types="node" />
import * as frontend from 'llparse-frontend';
export interface IBitcodeCompilerOptions {
    readonly debug?: string;
}
export declare class BitcodeCompiler {
    private readonly options;
    constructor(container: frontend.Container, options: IBitcodeCompilerOptions);
    compile(info: frontend.IFrontendResult): Buffer;
}
