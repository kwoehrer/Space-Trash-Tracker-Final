import * as frontend from 'llparse-frontend';
import { Compilation, IRBasicBlock, IRValue } from '../compilation';
import { Transform } from './base';
export declare class ToLowerUnsafe extends Transform<frontend.transform.ToLowerUnsafe> {
    build(ctx: Compilation, bb: IRBasicBlock, value: IRValue): IRValue;
}
