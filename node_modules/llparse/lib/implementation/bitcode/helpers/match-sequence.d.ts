/// <reference types="node" />
import * as frontend from 'llparse-frontend';
import { Compilation, IRDeclaration } from '../compilation';
import { Transform } from '../transform';
declare type TransformWrap = Transform<frontend.transform.Transform>;
export declare class MatchSequence {
    private readonly transform;
    private maxSequenceLen;
    private cachedFn;
    constructor(transform: TransformWrap);
    addSequence(sequence: Buffer): void;
    preBuild(ctx: Compilation): IRDeclaration;
    build(ctx: Compilation): void;
    private buildBody;
    private buildIteration;
    private ret;
    private reset;
}
export {};
