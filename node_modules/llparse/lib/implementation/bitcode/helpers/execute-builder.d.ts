import * as frontend from 'llparse-frontend';
import { Compilation } from '../compilation';
export declare class ExecuteBuilder {
    build(ctx: Compilation, info: frontend.IFrontendResult): void;
    private checkLingeringError;
    private restartSpans;
    private executeSpans;
    private buildError;
}
