"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert = require("assert");
const operand_1 = require("./operand");
class VBR extends operand_1.Operand {
    constructor(width) {
        super();
        this.width = width;
    }
    encode(writer, value) {
        // TODO(indutny): How to check that the type is VBRValue?
        assert(typeof value === 'number' || Array.isArray(value), 'Fixed encoding expected VBR number value');
        writer.writeVBR(value, this.width);
    }
}
exports.VBR = VBR;
//# sourceMappingURL=vbr.js.map