"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert = require("assert");
const base_1 = require("./base");
// TODO(indutny): optimization (nsw, nuw, ...)
class Binop extends base_1.Instruction {
    constructor(binopType, left, right) {
        super(left.ty, 'binop.' + binopType, [left, right]);
        this.binopType = binopType;
        this.left = left;
        this.right = right;
        assert(left.ty.isInt() && right.ty.isInt(), 'Only Int type is supported at the moment');
        assert(left.ty.isEqual(right.ty), 'Left and right operands of Binop have different types');
    }
}
exports.Binop = Binop;
//# sourceMappingURL=binop.js.map