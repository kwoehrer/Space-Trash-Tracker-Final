"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert = require("assert");
const constants_1 = require("../../constants");
const types_1 = require("../../types");
const base_1 = require("./base");
class Branch extends base_1.Instruction {
    constructor(condition, onTrue, onFalse) {
        super(new types_1.Void(), 'branch', [condition, onTrue, onFalse]);
        this.condition = condition;
        this.onTrue = onTrue;
        this.onFalse = onFalse;
        assert(condition.ty.isInt(), 'Branch `condition` must have Int type');
        assert.strictEqual(condition.ty.toInt().width, constants_1.BOOL_WIDTH, 'Branch `condition` must have a boolean width');
    }
}
exports.Branch = Branch;
//# sourceMappingURL=branch.js.map