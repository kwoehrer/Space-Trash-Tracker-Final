"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert = require("assert");
const base_1 = require("./base");
const common_1 = require("./common");
// TODO(indutny): support more indices?
class InsertValue extends base_1.Instruction {
    constructor(aggr, element, index) {
        super(aggr.ty, 'insertvalue', [aggr, element]);
        this.aggr = aggr;
        this.element = element;
        this.index = index;
        const fieldType = common_1.getAggrFieldType(aggr.ty, index);
        assert(element.ty.isEqual(fieldType), 'element type doesn\'t match field type in `insertvalue`');
    }
}
exports.InsertValue = InsertValue;
//# sourceMappingURL=insertvalue.js.map