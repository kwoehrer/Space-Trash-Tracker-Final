"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert = require("assert");
const constants_1 = require("../../constants");
const types_1 = require("../../types");
const base_1 = require("./base");
class ICmp extends base_1.Instruction {
    constructor(predicate, left, right) {
        super(new types_1.Int(constants_1.BOOL_WIDTH), 'icmp', [left, right]);
        this.predicate = predicate;
        this.left = left;
        this.right = right;
        assert(left.ty.isInt() || left.ty.isPointer(), 'Only integer types are supported at the moment');
        assert(left.ty.isEqual(right.ty), 'Left and right operands of ICmp have different types');
    }
}
exports.ICmp = ICmp;
//# sourceMappingURL=icmp.js.map