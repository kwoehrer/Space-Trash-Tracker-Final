"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const base_1 = require("./base");
class ArrayVal extends base_1.Constant {
    constructor(ty, elems) {
        super(ty);
        this.elems = elems;
    }
    isEqual(to) {
        if (this === to) {
            return true;
        }
        if (!to.isArray() || !to.ty.isEqual(this.ty)) {
            return false;
        }
        const toArray = to;
        return toArray.elems.length === this.elems.length &&
            toArray.elems.every((elem, i) => elem.isEqual(this.elems[i]));
    }
    toString() {
        const elems = this.elems.map((elem) => elem.toString());
        return `[const.array elems={${elems.join(', ')}}]`;
    }
}
exports.Array = ArrayVal;
//# sourceMappingURL=array.js.map