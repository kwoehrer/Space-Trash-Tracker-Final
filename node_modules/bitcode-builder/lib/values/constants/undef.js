"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const base_1 = require("./base");
class Undef extends base_1.Constant {
    constructor(ty) {
        super(ty);
    }
    isEqual(to) {
        if (this === to) {
            return true;
        }
        if (!to.isUndef()) {
            return false;
        }
        const toUndef = to;
        return toUndef.ty.isEqual(this.ty);
    }
    toString() {
        return `[undef type=${this.ty.typeString}]`;
    }
}
exports.Undef = Undef;
//# sourceMappingURL=undef.js.map